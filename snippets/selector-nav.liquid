
<ul class="selector-nav flex-centered">
  {% for block in section.blocks %}
    {% if block.type == 'featured_collection' %}
    <li class="selector-nav-item flex-centered" data-collection-url="{{block.settings.collection.url}}" {{ block.shopify_attributes }}>
        {{block.settings.collection.title}}
    </li>
    {% endif %}
  {% endfor %}
<ul>
